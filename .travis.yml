os: linux
dist: trusty
language: python
services: docker

script:
  - docker --version
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_USERPASS"
  # Build only if the commit range of a push contains Dockerfile or .travis.yml
  - '[[ "$TRAVIS_EVENT_TYPE" == "push" && -n "$TRAVIS_COMMIT_RANGE" &&
        -z "$(git diff --name-only $TRAVIS_COMMIT_RANGE |
            grep -e Dockerfile -e .travis.yml -e image -e conf)" ]] ||
     (docker build --tag x11vnc/desktop:zh_CN . &&
      docker push x11vnc/desktop:zh_CN &&
      touch success)'
  # Test scripts
  - 'git clone --depth 1 https://github.com/x11vnc/docker-desktop master &&
     master/ubuntu_desktop.py -d -n -t zh_CN && docker stop $(docker ps -q)'
  - docker images
